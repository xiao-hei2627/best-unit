export enum Locale {
  ZH = "zh",
  EN = "en",
}

export enum Theme {
  WHITE = "white",
  DARK = "dark",
}

export enum Env {
  DEV = "dev",
  DEVELOPMENT = "development",
  TEST = "test",
  PROD = "prod",
  PRODUCTION = "production",
}
