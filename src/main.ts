import { npmTest, printCurrentTime } from "./index.ts";
import "./components/business/ModalForm";

// 可选：导出组件列表或版本信息
export const components = ["x-greeting", "x-best-modal-form"];
export { npmTest, printCurrentTime };
